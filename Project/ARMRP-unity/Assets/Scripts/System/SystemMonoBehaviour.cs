using UnityEngine;

namespace Battle
{
    public class SystemMonoBehaviour : MonoBehaviour
    {
        //System 负责循环处理 更新状态 与数据
        //Manager 负责添加、删除对象

        //internal void OnValidate()
        //{
         
        //    //每次修改值就会移动到末尾，不好。   
        //移动脚本可以通过 制作Prefab 时通过添加顺序进行
        //    while (UnityEditorInternal.ComponentUtility.MoveComponentDown(this))
        //    {
        //    }
        //}
    }
}