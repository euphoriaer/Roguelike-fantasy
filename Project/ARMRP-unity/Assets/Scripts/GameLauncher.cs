using Battle;
using Battle.UI;
using UnityEngine;

public class GameLauncher : MonoBehaviour
{

    //加载资源管理。

    //加载UI

    //场景加载/卸载
    public void Start()
    {
        //UISystem uISystem = new UISystem();
        //Launcher 只需要通过各类Manager 添加/删除对象，其余操作System 自动进行（更新数据，更新UI）
        //uISystem.CreatePanel<BottomPanel>("");
    }
}